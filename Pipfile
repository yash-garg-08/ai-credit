[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
fastapi = ">=0.115.0"
"uvicorn[standard]" = ">=0.32.0"
"sqlalchemy[asyncio]" = ">=2.0.36"
asyncpg = ">=0.30.0"
pydantic = ">=2.10.0"
pydantic-settings = ">=2.6.0"
python-jose = {extras = ["cryptography"], version = ">=3.3.0"}
bcrypt = ">=4.2.0"
httpx = ">=0.28.0"
temporalio = ">=1.9.0"
redis = ">=5.2.0"
alembic = ">=1.14.0"
python-multipart = ">=0.0.12"
email-validator = ">=2.2.0"
cryptography = ">=41.0.0"

[dev-packages]
pytest = ">=8.3.0"
pytest-asyncio = ">=0.24.0"
aiosqlite = ">=0.20.0"

[requires]
python_version = "3.13"

[scripts]
dev = "uvicorn app.main:app --reload"
worker = "python -m app.workflows.worker"
seed = "python -m scripts.seed"
test = "pytest tests/ -v"
migrate = "alembic upgrade head"
